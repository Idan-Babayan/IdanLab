# build output
dist/
# generated types
.astro/
# wrangler / cloudflare pages local dev cache
.wrangler/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# local-only design study tools (kept out of source on purpose: this one loads faces from the
# Google Fonts CDN, and source carries no Google Fonts origin. See DECISIONS 2026-07-25)
typography-playground.html

# local-only design study, same reasoning: the HackTheBox difficulty palette study lives under public/
# so the dev server serves it with the self-hosted fonts (2026-09-07). Never a source file.
public/htb-difficulty-palette-study*.html

# local-only audit reports (owner and assistant working notes, 2026-09-13). Not part of the site and
# never published; the routing they carry lands as commits, the reports themselves stay on this machine.
audit/

# local-only Claude Code command briefs (owner working process, kept off the public repo on
# purpose, 2026-09-14). .claude/launch.json stays tracked; only the briefs are local.
.claude/commands/
